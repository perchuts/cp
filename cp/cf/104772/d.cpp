#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0); cin.tie(0);
#define sz(x) (int) x.size()
#define all(x) begin(x), end(x)
#define pb push_back
#define int long long

using namespace std;

using ll = long long;

int32_t main() {_
	int n; cin >> n;
	if (n == 1000) {
		for (int i = 0; i < 111; ++i) cout << 9;
		cout << 100000 << endl;
		exit(0);
	}
	for (int i = 0; i < n; ++i) {
		cout << n;	
	}
}
